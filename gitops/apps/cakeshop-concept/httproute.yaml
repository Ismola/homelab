apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: cakeshop-concept
  namespace: cakeshop-concept
spec:
  parentRefs:
    - name: traefik
      namespace: gateway-system
  hostnames:
    - "cakeshop-concept.ismola.dev"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: cakeshop-concept
          port: 80
