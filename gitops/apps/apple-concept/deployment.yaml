apiVersion: apps/v1
kind: Deployment
metadata:
  name: apple-concept
  namespace: apple-concept
  labels:
    app: apple-concept
spec:
  revisionHistoryLimit: 2
  progressDeadlineSeconds: 120
  selector:
    matchLabels:
      app: apple-concept
  template:
    metadata:
      labels:
        app: apple-concept
    spec:
      containers:
        - name: apple-concept
          image: ghcr.io/ismola/apple-concept:main
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
              ephemeral-storage: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
              ephemeral-storage: 256Mi
