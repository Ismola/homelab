apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pgadmin-cloudflare
spec:
  parentRefs:
    - name: traefik
      namespace: gateway-system
  hostnames:
    - "pgadmin.ismola.dev"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: pgadmin
          port: 80
